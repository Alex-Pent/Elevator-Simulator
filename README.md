# Elevator-Simulator

Thread based elevator simulation.

Uses the following algorithm:
If there is a called floor in the direction the elevator is going, go there
Otherwise, if there is a called floor in the opposite direction, change directions and go there
Otherwise, go to floor 0
